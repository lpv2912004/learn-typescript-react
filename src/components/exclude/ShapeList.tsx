import Shape from '@/components/exclude/Shape';

const ShapeList = () => {
    return (
        <div>
            <Shape color='light-red' key={1} />
        </div>
    );
};

export default ShapeList;
